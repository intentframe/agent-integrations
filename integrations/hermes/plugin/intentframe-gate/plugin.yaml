name: intentframe-gate
version: 0.2.1
description: "IntentFrame validate-only gate for governed Hermes tools — requires reason, calls adapter before side effects."
author: "IntentFrame Integrations"
requires_env:
  - IF_AGENT_ADAPTER_SOCKET
